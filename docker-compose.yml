version: '3.4'
services:
  rocketpool.avado.dnp.dappnode.eth:
    image: 'rocketpool.avado.dnp.dappnode.eth:0.0.56'
    build:
      context: ./build
      args:
        PRYSM_VERSION: v2.0.5
        RP_VERSION: v1.1.3
    volumes:
      - 'rocketpool:/rocketpool'
    environment:
      - NETWORK=prater
      - EXTRA_OPTS=
volumes:
  rocketpool: {}
