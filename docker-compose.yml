version: '3.4'
services:
  rocketpool.avado.dnp.dappnode.eth:
    image: 'rocketpool.avado.dnp.dappnode.eth:0.0.27'
    build:
      context: ./build
      args:
        RP_VERSION: v1.1.2
        PRYSM_VERSION: v2.0.5
    volumes:
      - 'rocketpool:/rocketpool'
    environment:
      - EXTRA_OPTS=
volumes:
  rocketpool: {}
